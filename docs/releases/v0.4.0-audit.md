# Signal Market Bot v0.4 — Reality Audit & Verification Report

**Audit Completed**: 2026-09-23  
**Target Release**: `v0.4.0`  
**Branch**: `feat/ai-platform-v0.4` (PR #7 -> `main`)  
**Auditor**: Principal AI Engineer & Staff Backend Engineer  
**Policy**: Strict verification without marketing embellishments. Status determined strictly by source code paths, dependency injection wiring, unit tests, integration tests, and live smoke probes.

---

## 1. Quality Gate Summary

| Gate Area | Benchmark / Metric | Result | Status |
| :--- | :--- | :--- | :--- |
| **Backend Unit & Integration** | `pytest backend/tests/ -v` | **109 / 109 Passed** (100%) | `PASSED` |
| **Backend Static Analysis** | `ruff check app tests evals` | **0 Errors** | `PASSED` |
| **Backend Formatting** | `ruff format --check app tests evals` | **108 files formatted** | `PASSED` |
| **Backend Bytecode Compilation** | `python -m compileall app` | **100% Compiled** | `PASSED` |
| **Deterministic AI Evals** | `python evals/run_evals.py` (32 Golden Cases) | **32 / 32 Passed** (100% Decision Accuracy) | `PASSED` |
| **Frontend Static Typing** | `npx tsc --noEmit` | **0 Type Errors** | `PASSED` |
| **Frontend Linter** | `npm run lint` | **0 Errors** (31 warnings acknowledged) | `PASSED` |
| **Frontend Component Tests** | `vitest run --run` | **18 / 18 Passed** (5 suites) | `PASSED` |
| **Frontend Production Build** | `npm run build` (Vite) | **0 Errors** | `PASSED` |
| **External Live AI Gateway** | `python evals/live_smoke.py` | `EXTERNAL CREDENTIAL ACCESS BLOCKER` (No `LIVE_AI_API_KEY`) | `EXTERNAL_NOT_VALIDATED` |

---

## 2. Reality Status Matrix

| Capability | Initial Audit | Final Status | Classification | Production Wiring | Verification Evidence | Notes & Verifications |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **AgentRuntime** | `MOCK_ONLY` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/runtime/factory.py` | `test_production_runtime_does_not_use_fake_providers` | Fake singleton eliminated; runtime assertion forbids FakeLLM in production; DB config drives live adapters. |
| **LLM Provider** | `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `OpenAICompatibleProvider` | `test_production_runtime_does_not_use_fake_providers` | Connected to `ai_api_base_url` & `ai_model`; handles timeouts, model detection, and custom credentials. |
| **Copilot** | `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/api/conversations.py` | `test_copilot_mode_uses_same_agent_runtime` | Shared `AgentRuntime.run()`; generates `AISuggestion` linked to `AIRun`; admin edit/send with provenance. |
| **Auto AI** | `BROKEN` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/services/message_handler.py` | `test_auto_mode_uses_agent_runtime` | Unified on `AgentRuntime.run()`; logs `AIRun`; manual takeover race check verified. |
| **RAG Retrieval** | `MOCK_ONLY` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/rag/retrieval.py` | `test_real_qdrant_adapter_roundtrip`, `test_group_cannot_retrieve_user_rag` | Scope filtering strictly enforces isolation; reindex pipeline rebuilds index from DB. |
| **Embedding Provider**| `MOCK_ONLY` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `OpenAICompatibleEmbeddingProvider`| `test_production_runtime_does_not_use_fake_providers` | Dynamic embedding adapter configured from DB settings; degraded mode on missing keys. |
| **Qdrant Vector DB** | `MOCK_ONLY` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/rag/qdrant_store.py` | `test_real_qdrant_adapter_roundtrip` | Real Qdrant adapter with deterministic UUID mapping and `query_points` retrieval. |
| **Scoped Memory** | `BROKEN` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/memory/providers.py` | `test_same_user_phone_uuid_share_memory`, `test_group_prompt_contains_no_private_user_memory` | Tied to canonical user ID; group isolation prevents loading private memories; `delete()` returns False if missing. |
| **Progressive Skills**| `PARTIAL` | **`VERIFIED`** | `VERIFIED_UNIT` | `app/ai/skills/registry.py` | `evals/run_evals.py` (32 cases) | Progressive loading injects only active skill instructions into prompt; router falls back cleanly. |
| **Governed Tools** | `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/tools/registry.py` | `test_mcp_write_requires_approval` | Hardcoded arguments removed; write/sensitive tools trigger `draft_for_human` supervisor review. |
| **MCP Integration** | `MOCK_ONLY` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/mcp/client.py` | `test_mcp_agent_readonly_execution_e2e`, `test_mcp_agent_write_tool_governed_blocking` | Official Python `mcp` SDK stdio subprocess client session, dynamic discovery, session injection isolation, and permission gating. |
| **Learning Loop** | `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/learning/curation.py` | `test_ai_platform.py` | Approved/edited copilot suggestions generate `FeedbackEvent` and `LearningCandidate`. |
| **AI Evaluation** | `VERIFIED (5)`| **`VERIFIED (32)`**| `VERIFIED_UNIT` | `evals/run_evals.py` | 32/32 cases passing (100% accuracy) | Deterministic contract evaluation suite expanded to 32 invariants covering safety, injection, handoff, RAG. |
| **AI Studio Console** | `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/api/ai_studio.py` | `test_ai_dashboard_has_no_hardcoded_metrics` | Hardcoded hit rates removed; dynamic calculation from `AIRun`; real diagnostics endpoint added. |
| **Message Provenance**| `PARTIAL` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/models/conversation.py` | `test_auto_mode_uses_agent_runtime`, `test_copilot_mode_uses_same_agent_runtime` | `Message.origin` (`ai_auto`, `human_ai_assisted`, `human_manual`) with `ai_run_id` linkage. |
| **Privacy Isolation** | `BROKEN` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/ai/rag/retrieval.py` | `test_group_cannot_retrieve_user_rag`, `test_group_prompt_contains_no_private_user_memory` | P0 Invariant enforced: Group turns strictly reject user scope retrieval and user memory loading. |
| **Signal Zero-Env** | `UNTESTED` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/services/signal_client.py` | `test_signal_zero_env.py` (2/2 passed) | Background listener starts/stops dynamically based on DB settings without process restart or env vars. |
| **API Contract Drift**| `DRIFTED` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/api/ai_studio.py`, `frontend/src/api.ts` | `test_api_contract_drift.py` (3/3 passed) | Zero drift across backend routes, OpenAPI schemas, and frontend client DTOs; verified with real HTTP smoke tests. |
| **Privacy Erasure** | `MISSING` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/services/data_retention.py` | `test_canonical_user_purge_covers_all_artifacts`, `test_privacy_erasure_fails_closed_on_vector_deletion_failure` | Canonical user purge across relational rows and vector items; fails closed with rollback on vector deletion errors. |
| **Master Key & Migration** | `DEGRADED` | **`VERIFIED`** | `VERIFIED_INTEGRATION` | `app/services/runtime_config.py` | `test_persistent_master_key_and_legacy_migration` | Persistent master key in `./data/.master_key` (`0600` permissions); automatic transparent re-encryption of legacy ciphertext. |
| **External Live AI** | `UNTESTED` | **`EXTERNAL_NOT_VALIDATED`** | `EXTERNAL_NOT_VALIDATED` | `evals/live_smoke.py` | `evals/live_smoke.py` | External live gateway validation is blocked due to unsupplied `LIVE_AI_API_KEY`. Accurately halts without fake pass. |

---

## 3. Priority 0 / Priority 1 Remediation Log

| Issue ID | Severity | Description | Status | Verification Evidence |
|---|---|---|---|---|
| **SEC-01** | **P0** | Group chat context retrieved and leaked user private RAG notes. | **FIXED** | In `retrieval.py`, `allowed_scopes.discard("user")` when `is_group=True`. Verified by `test_group_cannot_retrieve_user_rag`. |
| **SEC-02** | **P0** | Group chat context loaded private user memory into LLM prompt. | **FIXED** | In `agent_runtime.py`, loading user memory skipped when `context.is_group=True`. Verified by `test_group_prompt_contains_no_private_user_memory`. |
| **ARCH-01** | **P0** | Production runtime defaulted to `FakeLLMProvider` singleton. | **FIXED** | Deleted global fake singleton; added `create_agent_runtime` factory reading DB config; added runtime assertion prohibiting Fake in production. |
| **ARCH-02** | **P0** | Auto AI routed to legacy `AIEngine`, while Copilot routed to `AgentRuntime`. | **FIXED** | Unified `MessageHandler` on `AgentRuntime.run()` for both Auto and Copilot; preserved manual race protection check. |
| **RAG-01** | **P0** | Production RAG used in-memory fake store instead of Qdrant. | **FIXED** | Implemented `QdrantVectorStore` with official `qdrant-client` `query_points` and UUID mapping. Verified by `test_real_qdrant_adapter_roundtrip`. |
| **MCP-01** | **P1** | MCP integration was dummy list without real SDK connection. | **FIXED** | Implemented real stdio client using official `mcp` Python SDK 2.x; verified by `test_mcp_agent_readonly_execution_e2e`. |
| **MCP-02** | **P1** | MCP tools were incorrectly injected with SQLAlchemy `AsyncSession`, breaking JSON-RPC serialization. | **FIXED** | In `ToolRegistry.execute`, only non-MCP tools receive `session`; verified in `test_mcp_agent_readonly_execution_e2e`. |
| **SIG-01** | **P1** | Signal client instance attributes were not updated on runtime config reload, preventing zero-env startup. | **FIXED** | Maintained `_api_url`, `_phone_number`, `_api_token` in `SignalClient.apply_runtime_config`; verified in `test_signal_zero_env.py`. |
| **MET-01** | **P1** | AI Studio hardcoded `rag_hit_rate = 0.85` and fake confidence scores `0.92/0.99`. | **FIXED** | Removed hardcoded constants; metrics computed dynamically from persisted `AIRun` traces; `confidence` defaults to None with clear `decision_reason`. |
| **MEM-01** | **P1** | `NativeMemoryProvider.delete()` unconditionally returned `True`. | **FIXED** | Fixed `delete()` to check `rowcount > 0` and return `False` when record is missing. |
| **TOOL-01** | **P1** | Hardcoded destructive refund arguments (`order_id=1, amount=25.0`) in graph. | **FIXED** | Removed hardcoded values; validated arguments extracted from context or flagged for admin approval. |
| **KEY-01** | **P1** | Encryption key fell back to `jwt_secret_key` on restart if environment variable was missing. | **FIXED** | Implemented persistent `./data/.master_key` (`0600`) and startup migration for legacy ciphertext; verified in `test_persistent_master_key_and_legacy_migration`. |
| **API-01** | **P1** | API contract drift between FastAPI routes and frontend client DTOs. | **FIXED** | Added missing endpoints (`DELETE /knowledge/documents/{id}`, `POST /mcp/servers/{id}/disconnect`, `POST /mcp/servers/{id}/toggle`, `GET /evals/runs`); verified in `test_api_contract_drift.py`. |
