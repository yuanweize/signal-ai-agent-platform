services:
  backend:
    image: signal-ai-agent-platform-backend:${APP_VERSION:-dev}
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}

  frontend:
    image: signal-ai-agent-platform-frontend:${APP_VERSION:-dev}
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}
